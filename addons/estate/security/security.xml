<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Group: Agent -->
    <record id="estate_group_user" model="res.groups">
        <field name="name">Agent</field>
        <field name="category_id" ref="base.module_category_real_estate_brokerage"/>
    </record>

    <!-- Group: Manager (inherits Agent group) -->
    <record id="estate_group_manager" model="res.groups">
        <field name="name">Manager</field>
        <field name="category_id" ref="base.module_category_real_estate_brokerage"/>
        <field name="implied_ids" eval="[(4, ref('estate_group_user'))]"/>
    </record>

    <!-- Record Rule: Limit Agents to Only Their Properties or Unassigned Properties -->
    <record id="estate_property_rule_agent" model="ir.rule">
        <field name="name">Agent: Can See and Modify Only Their Properties</field>
        <field name="model_id" ref="model_estate_property"/>
        <field name="domain_force">['|', ('salesperson_id', '=', False), ('salesperson_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('estate_group_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Record Rule: Managers Have Full Access -->
    <record id="estate_property_rule_manager" model="ir.rule">
        <field name="name">Manager: Full Access to All Properties</field>
        <field name="model_id" ref="model_estate_property"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('estate_group_manager'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
</odoo>
